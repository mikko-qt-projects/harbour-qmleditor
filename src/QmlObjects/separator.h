#ifndef SEPARATOR_H
#define SEPARATOR_H

#include <QWidget>
#include "qmlobject.h"

class Separator : public QmlObject
{
    Q_OBJECT
public:
    explicit Separator(QWidget *parent = 0);

signals:

public slots:
};

#endif // SEPARATOR_H
